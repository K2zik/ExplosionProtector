name: ExplosionProtector
version: '2.0'
main: org.SlidrusForeal.explosionProtector.ExplosionProtector
api-version: '1.21'
depend:
  - CoreProtect
folia-supported: true

commands:
  ep:
    description: ExplosionProtector commands (status, info, language)
    usage: /ep <status|info|language>
    aliases:
      - explosionprot
